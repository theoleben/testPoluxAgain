<form method="post" action="{{ path('app_admin_subscription_delete', {'id': subscription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscription.id) }}"><br>
    <button class="btn sm-btn btn-secondary">Supprimer</button><br>
</form>
