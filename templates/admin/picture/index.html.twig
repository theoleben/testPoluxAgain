{% extends 'base.html.twig' %}

{% block title %}Picture index{% endblock %}

{% block body %}
    <h1>Picture index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom du jeu</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {# dans la class picture ont veut un truc une donnée/ propriété. #}
        {% for picture in pictures %} 
            <tr>
                <td>{{ picture.id }}</td>
                {# creation d'une ternaire car si un jeu n'a pas de titre, doit indiquer une erreur. #}
                <td>{{ picture.game ? picture.game.title : "aucun jeu" }}</td>  
               
                <td><img src="{{ '/images/'~ picture.name }}" alt="{{ picture.id }}" width="100px" height="100px"></td>
                {# <td><img src="{{'/images/' ~ picture.name }}" alt="" height="100px"></td>  #}

                <td>
                    <a href="{{ path('app_admin_picture_show', {'id': picture.id}) }}"><button class="btn">{{ button_label|default('Voir la photo') }}</button></a><br>
                    <a href="{{ path('app_admin_picture_edit', {'id': picture.id}) }}"><button class="btn">{{ button_label|default('Modifier') }}</button></a>
                    {{ include('admin/picture/_delete_form.html.twig') }}
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Pas de photo trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_picture_new') }}"><button class="btn">{{ button_label|default('Ajouter une photo') }}</button></a>
{% endblock %}
