{% extends 'base.html.twig' %}

{# {{ picture.id }} permet d'afficher l'id de la photo #}
{% block title %}Editer la photo N° {{ picture.id }} {% endblock %}

{% block body %}
    <h1>Editer la photo N° {{ picture.id}} </h1>

    {{ include('admin/picture/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_admin_picture_index') }}"><button class="btn">{{ button_label|default('Retourner a la liste') }}</button></a>

    {{ include('admin/picture/_delete_form.html.twig') }}
{% endblock %}
