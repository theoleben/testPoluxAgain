{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category_index') }}"><button class="btn">{{ button_label|default('Retour à la liste') }}</button></a>

    <a href="{{ path('app_admin_category_edit', {'id': category.id}) }}"><button class="btn">{{ button_label|default('Modifier') }}</button></a>

    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}
