{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <h1>Modifier mes informations</h1>
   
    {{ include('profil/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('profil/_delete_form.html.twig') }}<br>

    {# <a class="btn btn-secondary" href="{{ path('app_profil_index') }}">Modifier</a> #}

    <a class="btn btn-secondary" href="{{ path('app_profil_index') }}">Retourner à mon profil</a><br>

    

{% endblock %}
