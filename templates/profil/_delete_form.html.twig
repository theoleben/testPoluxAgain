<form method="post" action="{{ path('app_profil_delete', {'id': app.user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app.user.id) }}">
    <br>
    <button class="btn  btn-sm btn-secondary">Supprimer mon compte</button>
</form>
